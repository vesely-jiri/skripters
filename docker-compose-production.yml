services:

  #-------- frontend services---------------------------------------------
  frontend:
    build:
      args:
        NODE_ENV: production

  #-------- backend services---------------------------------------------
  skripters-api-gateway:
    environment:
      - SPRING_PROFILES_ACTIVE=production
    env_file:
      - .env.production

  skripters-auth:
    environment:
      - SPRING_PROFILES_ACTIVE=production
    env_file:
      - .env.production